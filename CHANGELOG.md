# Changelog

## [Unreleased]

### Fixed
- サマリーの見出しを「保有者数」から「保有銘柄数」に修正 (#33)
- 取得金額の合計が0のとき `print_summary` が `ZeroDivisionError` で落ちる不具合を修正 (#34)
- 桁区切りカンマ付きの数値を含む行が無言でスキップされる不具合を修正 (#35)
- パース不能なデータ行を `UserWarning` で通知するようにした。従来は無言でスキップしていた (#36)
- 半角括弧のセクション見出しで口座区分が全件 `Unknown` になる不具合を修正 (#37)
- `株式（` で始まらないセクション（`外国株式（…）` など）で直前セクションの口座区分を引きずる不具合を修正。あわせて口座区分の判定で NISA を特定より先に見るようにした (#38)
- ハードコードした3プレフィックス以外の投資信託名が、空白で証券コードと銘柄名に誤分割される不具合を修正 (#39)
- 損益率の分母を取得金額に統一。行（評価額基準）と合計（取得金額基準）で定義が食い違っていた。**損益率の表示値が変わります** (#40)
- ターミナル表示と Markdown 出力で別実装になっていた合計の算出を一本化。出力値は変わらない (#41)
- main への push で release ジョブが TestPyPI への重複アップロードにより毎回失敗していたのを修正 (#44)

### Changed
- テストフィクスチャを内部整合するよう再生成。明細・セクション合計・総合計が互いに矛盾しない状態にした (#53)
- ADR を `docs_src/` から `adr/` に移動。GitHub Pages は利用マニュアルという位置づけのため、開発経緯の記録をデプロイ対象から外した

### Added
- API リファレンスに `sbipf_reporter.summary` モジュールを追加（#41 で追加した際の記載漏れ）
- `Holding.acquisition_cost` プロパティ（取得金額） (#40)
- ADR-0001: 損益率の分母を取得金額に統一する決定を記録 (#40)
- `sbipf_reporter.summary` モジュール（`PortfolioSummary` / `summarize`）。ターミナル表示とMarkdown出力が同じ集計実装を共有する (#41)
- ADR-0002: 合計算出の一本化の決定を記録 (#41)
- GitHub Issue Forms のテンプレートを追加（バグ報告 / 機能要望 / 作業タスク）。
  バグ報告のみリポジトリ固有版、機能要望と作業タスクはアカウント共通 (Chronona/.github) の複製

### Documentation
- README.md を充実（バッジ・機能一覧・出力例・対応フォーマット・ライセンスを追加）
- docstring 強化（公開関数すべてに Args/Returns を記載）
- CHANGELOG.md を新規作成

## [0.1.1] - 2026-07-05

### Fixed
- 文字コード自動判別対応（utf-8-sig / cp932 / utf-8 の fallback）
- 【】で囲まれたセクションヘッダーに対応
- 10列CSVフォーマット（参考単価なし）に対応し、列レイアウトを自動検出

### Added
- `__main__.py` 追加（`python -m sbipf_reporter` 対応）
- Conventional Commits による自動バージョンアップ CI ワークフロー

## [0.1.0] - 2026-07-05

### Added
- SBI証券CSVパーサーの実装
- CLIレポート出力機能
- レポート出力形式の拡張（Markdown / CSV対応）
- プロジェクト構成と CI 環境の整備（pytest / ruff / mypy）
